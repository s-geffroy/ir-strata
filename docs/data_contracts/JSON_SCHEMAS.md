# Contrats JSON

Les schémas sont stricts mais évolutifs.

- `additionalProperties: false` sur les objets critiques.
- Champ `experimental` autorisé pour extensions non stabilisées.
- `schema_version` obligatoire.
- `model_version` obligatoire sur les métadonnées.

Les schémas physiques sont dans `/schemas`.
